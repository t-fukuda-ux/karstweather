# 天気予報システム 変更記録（2026-09-07 夕方〜2026-09-10）

作業場所: 開発PC（`C:\Users\user\Claude\Projects\weather-lowclouds`）
この記録は**サーバーPC側への引き継ぎ**を兼ねる。`変更記録_2026-09-07.md` の続き。

---

## 0. サーバーPCで最初にやること

**カメラ記録のタスクが動いていない可能性が高い。** 3日経っても `camera/mezuru_contrast.csv` が
1行（2026-09-07 17:58 の1件）のままで、リモートに新しい記録が来ていない。

```powershell
git pull
ls camera                                                    # ① フォルダがあるか
Get-ScheduledTaskInfo -TaskName "KarstCameraCapture" | Select-Object LastRunTime,LastTaskResult,NextRunTime
Get-Content camera\mezuru_contrast.csv | Measure-Object -Line # ③ 3日動いていれば70行前後
git status --short camera/                                   # ④ 未コミットがあるか
```

- 未登録なら `powershell -ExecutionPolicy Bypass -File .\camera\install-camera-task.ps1`
- 登録直後に `.\camera\capture-mezuru.ps1` を1回手動実行し、CSVの行が増えることを確認する
- **記録ファイルは自動pushされない。** 溜まったら手動で送る（下記「運用ルール」参照）

---

## 1. 確定した変更

| コミット | 内容 |
|---|---|
| `62839ff` | `変更記録_2026-09-07.md` をリポジトリに取り込み。作業リポジトリの置き場所の方針を明記 |
| `bc80637` | **V_summit を850hPa主役に見直し、予報視程(V_vis)をVから外す** |
| `2d14333` | **ライブカメラのコントラストを毎時記録**（判定はしない） |
| `6aecdbd` / `1560abf` | 初回の実測照合結果と、V_top_fog の既知の欠陥を記録 |

### 1-1. V_summit の係数を変更（`bc80637`）

```
変更前                        変更後
上下とも90%以上 → 0.00        上下とも90%以上 → 0.00
下側のみ90%以上 → 0.40        上側のみ90%以上 → 0.15  ★
上側のみ90%以上 → 0.70  ★    下側のみ90%以上 → 0.50
上下とも乾燥    → 1.00        上下とも乾燥    → 1.00
```

**上側（850hPa ≒ 1412m）を主役にした。** 展望地点の1400mとほぼ同じ高さで、格子の地表(1296m)より
100m以上高い。地上湿度は霧のさなかに76〜79%まで下がり、まったく捉えていなかった。

### 1-2. 予報視程(V_vis)を V から外した（`bc80637`）

**記録は残すが計算には使わない。** min に入れていると V_summit の減点を打ち消す。
2026-09-07 に3回外しており、特に**15〜16時は濃霧のさなかに 41,000m** を出していた。

⚠ **V_vis を V に戻してはいけない。** 変数の考え方が悪いのではなく、Open-Meteo の視程予報が
当たらない（実測値を入れれば V_vis は完璧に機能する）。倍率が6〜136倍とばらばらで補正もできない。

### 1-3. ライブカメラの記録（`2d14333`）

`https://www.karst.co.jp/cam/mezuru01.jpg`（姫鶴荘自身のカメラ）を毎時50分に取得し、
距離帯ごとのコントラストを記録する。**第1段階として記録のみ。霧の判定は入れていない。**

| ファイル | git | 備考 |
|---|---|---|
| `camera/capture-mezuru.ps1` | 追跡 | 取得と測定。失敗しても終了コード0 |
| `camera/install-camera-task.ps1` | 追跡 | タスク `KarstCameraCapture` を登録（`-Remove` で削除） |
| `camera/mezuru_contrast.csv` | **追跡** | 測定値。解析のためgitで共有する |
| `camera/images/` `camera/capture.log` | 除外 | 画像は1日約1.1MB・90日で間引く |

⚠ `System.Drawing` を使うため **GitHub Actions（Linux）では動かない。ローカル専用。**
⚠ 画像 960×540 前提で帯を決めている。**画角が変わったら帯を引き直す**（`note` 列に `size_changed` が出る）。

---

## 2. 現地実測で分かったこと（最重要）

2026-09-07 に**初めて現地実測5件**を得た。`unkai_log.csv` に記録済み。
**そのすべてが設計の欠陥を指した。** 詳細は `SYSTEM.md` 7-3節。

### 当たったもの

- **低層雲（平均版）の 20/40/50 の目安** … 公開ページで16-17時が51%、実際に濃霧。
  **この行は現行のままでよい。変更しないこと**
- **V_summit（850hPa主役に修正後）** … 霧だった全時刻が 0.00〜0.15

### 外したもの（モデル側の限界）

| 項目 | 予報 | 実測 |
|---|---|---|
| 予報視程（15-16時） | 41,000m | 濃霧 |
| 降水量（15-16時） | 0.00mm | 小雨（降水確率は71-74%と高かった） |
| 地上湿度（15-16時） | 76-79% | 「すごく高い」 |
| 雲頂の南北差 | 差なし（両方875hPa以上が湿潤） | **南1100〜1300m／北1500m** |

### 雨上がり型の雲海は測れないと判断した

理由は3つ。

1. **雲頂が分解能の隙間に来る。** 875hPa(1191m)と850hPa(1442m)の間隔が251mあり、
   南1100〜1300mと北1500mを区別できない。放射冷却型は雲頂が800〜1000m前後で
   900/875hPa に挟めるが、雨上がり型はちょうど測れない高さに来る
2. **展望地点が雲に入ったり出たりする。** 1時間刻みでは追えない
3. **モデルの地上値が実態から離れる。** 湿度も降水も外しており、「谷の湿度」を
   主材料にする設計が崩れる

→ **雨上がり型には点数を付けず、「雨上がりの雲海の可能性」という注意書きに留める方針**（未実装）。
指数は**放射冷却型に集中する**。

### カメラの指標設計（2026-09-10 に判明）

濃霧の実測が取れ、**「遠景/近景の比」は使えない**と分かった。濃霧では近景まで白く溶けるため、
分母が小さくなって比が逆に大きくなる。

| | 遠景 | 中景 | 近景 | 遠景/近景 | ファイル |
|---|---|---|---|---|---|
| 09-07 17:58（霧・数百m先は見える） | 20.0 | 29.6 | 40.1 | 0.500 | 46.6KB |
| 09-10 11:49（濃霧） | 16.7 | **8.6** | **11.2** | **1.491** | **19.7KB** |

**有効なのは「近景 sd の絶対値」と「ファイルサイズ」。** 近景 sd は 濃霧11.2 / 霧40.1 で
きれいに分かれる。ファイルサイズも 19.7KB / 46.6KB と有効（JPEGは白く均一なほど小さい）。
**晴天時の値がまだ無い。** これが取れれば境目を引ける。

---

## 3. 未実装・保留中（勝手に実装しないこと）

| 項目 | 状態 |
|---|---|
| 雨上がり型の第2経路 | **実装しない方針**。注意書きのみに留める（上記2章） |
| ラベルの大／中／小＋「雨上がり雲海」 | 保留。Fの区切りは 0.70／0.55／0.40 で試算済み |
| 評価時間帯の拡張（日の出+2h → +5h） | 保留。単独では効果が小さい（0点→29点止まり） |
| V_top_fog の欠陥修正 | **未修正**（下記4章） |
| 同期チェック用スクリプト | 提案のみ。作るか未定 |

---

## 4. 既知の欠陥（未修正）

### V_top_fog が途中の乾燥層で打ち切られる

`Get-UnkaiCloudTop` は下から順に見て `RH<90%` の面が出たら打ち切る。
2026-09-07 18時は**全4地点で875hPa以上が93〜100%の湿潤層**だったが、その下の
900hPa が85〜89%だったため連続が切れた。

- 美川・面河・梼原 → `no_moist_layer`（雲層があるのに検出できず）
- 津野町 → **雲頂を「706〜941m」と誤算出し V_top_fog=1.00**（乾燥層の下端を雲頂と誤認。根拠なし）

85〜89%は「乾いている」より「ほぼ湿っている」で、90%の単一しきい値では連続が途切れる。
**直すには「最も高い湿潤層の上端を探す」形に変える必要がある。**
現状 `V = min(V_top_fog, V_summit)` なので V_summit が霧を捉えている限り実害は出にくいが、
**両方が高く出る状況では指数を過大にする。**

⚠ **放射冷却型の検証を始める前に直しておくのが安全**（放射型では V_top_fog が主役になる）。

---

## 5. 運用ルール（変更あり）

### 作業リポジトリはクラウド同期フォルダに置かない

2026-09-07 に `H:\マイドライブ\...\karstweather` で **Drive が `.git/refs` 以下に
`desktop.ini` を17個作成し、`git fetch` が失敗**した。Drive上のクローンは削除済み。

```
各PC: C:\...\karstweather へ clone → 編集 → commit → push
                    ↕  GitHub（唯一の正）
```

仕様書も記録もリポジトリ内にあるので、`git pull` すればどのPCでも最新が手に入る。
**クラウド側にファイルのコピーを置かないこと。**

### 記録ファイルは手動で push する

`camera/mezuru_contrast.csv` と `unkai_log.csv` は**どの自動処理も push しない**
（`publish.ps1` と Actions の `git add` は生成HTMLと `forecast-history/` に限定）。

```powershell
git add camera/mezuru_contrast.csv unkai_log.csv
git commit -m "data: カメラと実績の記録"
git push
```

**1週間に1回程度で十分。** 溜まったら送る。これを忘れると開発PC側から解析できない
（実際に3日分が届いていなかった）。

### 実績の記録

`unkai_log.csv` に2軸で記録する。**具体的な数字が入ると効く。**

- 時刻と状態（霧／薄い霧／濃霧／雲海）
- **見える距離**（「数百m先は見えるが1km先は見えない」など）
- **雲頂の高さ**（「1100〜1300m」など。最も貴重な情報）
- 方角（南／北）

---

## 6. 次にやること

1. **カメラのタスクを動かし、記録を溜める**（0章）。特に**晴天時の画像**が基準値づくりに必要
2. **放射冷却型の好条件日の照合。** 指数の本命はまだ一度も検証できていない
   （2026-09-07 に観測できたのは雨上がり型だけ）
3. カメラの記録が1週間分溜まったら、晴天時と霧天時のコントラストの境目を出す
4. V_top_fog の欠陥修正（2の前に）
