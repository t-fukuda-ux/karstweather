# 天気予報システム 改善内容（2026-09-07）

状態：2026-09-07 公開反映完了。GitHub Actionsの予報生成・push・平均版の鮮度チェックが成功し、公開URLの表示も確認済み。
取得したリポジトリ： https://github.com/t-fukuda-ux/karstweather
作業場所：この文書と同じフォルダ。

## 実装した変更

2. 平均版の取得日時が3時間以上古ければGitHub Actionsを失敗状態にし、既存の通知設定で知らせる。成功した他の版は先に公開する。日時欠落や不正な未来日時も検知する。画面の更新日時表示は変更しない。
3. 雲海ラベルを「雲海期待・視界良好」「雲海期待・視界不良」「雲海期待・霧の恐れ」「雲海期待・雲頂不明」「雲海期待・視界不明」に短縮する。
4. 毎日18時以降、各モデルで最初に取得できた有効な翌朝予報を発表時刻付きで長期保存する。詳細値・気圧面・コードのリビジョン・平均版の代替状況を記録する。同日の既存予報は上書きしない。現地実績CSVには触れない。
5. 3版とも、週間の最高・最低気温を補正後の毎時気温から集計する。週間最高の一律+2℃を廃止する。欠測を0℃表示にしない。

関連修正：HTML保存失敗を生成成功扱いにしない。
仕様の更新：SYSTEM.md（このリポジトリ内）。最初に参照した親フォルダのSYSTEM.mdは変更していない。

## 確認済み

- PowerShell 7とWindows PowerShell 5.1で回帰テスト成功。
- 3時間の境界、日時欠落、気温の夏季補正、7日集計、欠測、雲海ラベル、履歴の時刻と上書き防止。
- 実APIで規定版・EC版・平均版・雲海検証ページをローカル生成。
- 3版とも週間カード7日分を確認。毎時表に載る全日の最高・最低と週間カードの値が一致。
- index.htmlと平均版の内容が一致。
- 変更したPowerShellの構文とUTF-8 BOMを確認。

## 反映後の運用

通知はGitHubの既存のActions失敗通知設定に従う。Actions自体が起動しない場合は検知できない。
長期履歴は forecast-history/ に保存して公開リポジトリへ追加する。18時より前に実行した分は夕方代表として保存しない。
forecast-runs/ は各実行のローカル履歴（git対象外）。Actionsでは実行終了後に消えるため、長期検証には forecast-history/ を使う。
長期履歴はリポジトリ・Pagesから閲覧可能。予報値のみで個人情報は含まない。

## 公開反映結果

- 修正コミット：6bd7c46
- 初回公開生成：d6bd317
- GitHub Actionsによる更新：6138493（公開予報の取得日時 2026-09-07 10:43 JST）
- 動作確認：https://github.com/t-fukuda-ux/karstweather/actions/runs/34073898779 （成功）
- 公開URL：https://t-fukuda-ux.github.io/karstweather/
- 夕方代表の長期履歴は、本日18時以降に有効な翌朝予報を取得した最初の実行から保存される。
- Google Driveが .git/refs にdesktop.iniを追加してgit fetchを妨げたため、同期対象外の一時コピーから公開した。作業フォルダのソースと生成HTMLは保持している。今後もgit操作は同期対象外のコピーで行う。

## 再テスト

```powershell
pwsh -NoProfile -ExecutionPolicy Bypass -File tests/regression.ps1
powershell.exe -NoProfile -ExecutionPolicy Bypass -File tests/regression.ps1
pwsh -NoProfile -ExecutionPolicy Bypass -File generate_all.ps1 -ForceUnkaiLab
pwsh -NoProfile -ExecutionPolicy Bypass -File check_forecast.ps1
```